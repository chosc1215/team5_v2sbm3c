<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dev.mvc.review.ReviewDAOInter">

  <insert id="create" parameterType="dev.mvc.review.ReviewVO">
    INSERT INTO review(reviewno, restcontentsno, memberno, recontent, rating, rdate) 
    VALUES(review_seq.nextval, #{restcontentsno}, #{memberno}, #{recontent}, #{rating}, sysdate)
  </insert>

</mapper>